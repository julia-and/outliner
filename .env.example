# Dexie Cloud sync endpoint. Defaults to the project's production tenant
# if unset. Override for staging, self-hosting, or local testing.
VITE_DEXIE_CLOUD_URL=https://zgpzaaasb.dexie.cloud

# Enable Dexie debug logging (`Dexie.debug = true`). Set to any non-empty
# string to enable; unset or empty to disable. Off by default.
# VITE_DEXIE_DEBUG=1
